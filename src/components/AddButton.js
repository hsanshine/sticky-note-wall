import react from "react";
import "./AddButton.css";
import "./Note.css";

const AddButton = () => {
  return (
    <div className="add-btn-case">
      <div className="add-btn">
        <button className="plus-sign"> + </button>
      </div>
    </div>
  );
};

export default AddButton;
